#include <cstdio>
#include <vector>
using namespace std;

int
main() {
        int n, k, c;
        scanf("%d %d", &n, &k);
        int arr[n];
        vector<int> vec;
        for (int i = 0; i < n; i++) {
            int a;
            scanf("%d", &a);
            vec.push_back(a);
        }
        c = b + i * k;
        for(int b = 1; b <= n; b++) {
            c = 
        }
        
        
        return 0;
}
}